import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'markdown_ionic_ref_impl',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
